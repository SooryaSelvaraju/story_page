<div class="button"><%= link_to "Home", stories_path %></div>
<div class="post">
<h2>Comment Section</h2>
<h2><%= @story.user %></h2>
<h3><%= @story.title %></h3>
<h5><%= @story.description %></h5>
<p>👍 <%= @story.upvotes %> likes</p>
<div class="button"><%= link_to "✐ Add Comments", new_comment_path(story_id: @story.id) %></div>


<div class="comment">
  <% @comments.each do |comment| %>
    <h2>●<%= comment.user %></h2>
    <h4><%= comment.content %></h4>
    <h5><%= comment.created_at %></h5>
    <div class="button"><%= link_to "✐ Add Comment", new_response_comment_path(comment_id: comment.id) %></div>

    <div class="response">
      <% response_comment = ResponseComment.where(comment_id: comment.id).order("created_at DESC") %>
      <% response_comment.each do |response| %>
        <div>
          <h2>・<%= response.user %></h2>
          <h5><%= response.content %></h5>
          <p><%= response.created_at %></p>
          <div class="button"><%= link_to "✐ Add Comment", new_response_comment_path(comment_id: comment.id, response_id: response.id) %></div>

        </div>
      <% end %>
    </div>
  <% end %>
</div>
</div>